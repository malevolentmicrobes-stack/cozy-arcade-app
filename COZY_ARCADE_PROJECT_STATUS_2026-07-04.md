# Cozy Arcade PHASE1 (cozy-app) — Project Status 2026-07-04

**LIVE, CONFIRMED (curl, not assumed):** `cozy-arcade-v111` — matches local, matches pushed. (v112 below, this entry, not yet pushed.)

## Clear Local State re-confirmed real + moved into Advanced ▾ dropdown (v111→v112)
User clarified the earlier revert was caution before shipping a destructive action, not a rejection: "OK TO CLEAR LOCAL STATE IF SELECTED THEN CONFIRMED." Restored `window.clearAllLocalProgress()` to actually clear `phase3State.progress`/`cozy_arcade_progress_v1` through the canonical save path, gated behind explicit menu selection + Accept/Cancel confirm.

Also redesigned placement per "avoid too much redundancy" feedback: removed the separate `<details id="advancedSettings351">` collapsible (redundant next to "How to Play"), folded Clear Local State into a new 4th "Advanced ▾" dropdown button in the same action row as Apply/Import ▾/Export ▾, reusing the exact existing `exportMenuWrap351`/`exportMenu351` pattern. Found and fixed one real layout bug while testing: the shared `.exportMenu351{right:0}` positioning clips off-screen left when this 4th button wraps to its own line at narrow widths — added a scoped `#advancedMenu351{right:auto;left:0}` override, screenshot-confirmed fixed, Import/Export untouched.

Live-validated: menu opens/closes correctly, closes on item click, confirm panel shows, Cancel leaves progress untouched (3 cards), Accept clears it (0), FSRS 17/17, smoke 6/6, zero errors. Shadow Dungeon timing/card-review-timing controls intentionally untouched — premortem only, per explicit "no quick changes" instruction.

## Clear Local State fix REVERTED same day (v110→v111) — user wants progress kept, not an easy delete path
User: "UNDO THIS: KEEP LOCAL STATE BUT MAKE SURE STATE CORRECT... I WANTED TO KEEP THE LOCAL STATE BUT HAVE AN OPTION IN THE SETTINGS." `window.clearAllLocalProgress()` is now a no-op (does not touch `phase3State.progress` or `cozy_arcade_progress_v1`); all three entry points (old `#review` button, old Shadow Dungeon hub button, new Advanced Settings drawer button) call it and all three now safely do nothing to real progress. UI scaffolding (Advanced Settings collapsible, Accept/Cancel confirm panel, the confirm() prompts on the two old buttons) left in place since it's harmless either way. Live-validated: played/rated 3 cards, tried all three clear entry points, progress count stayed at 3 through every one, FSRS 17/17, smoke 6/6, zero errors.

**Open question, asked back to user rather than guessed at a second time:** what should "make sure state correct" and "have an option in the settings" actually mean if not a destructive clear? Candidates not yet chosen between: (a) a read-only diagnostic panel showing current progress counts/health for verification, (b) an export-first-then-clear safety net, (c) something else. Not implemented until answered.

## Clear Local State fixed for real (v109→v110) — confirmed broken for 3 weeks, nobody caught it
User asked for a simple "Advanced Settings > Clear Local State" with a confirm prompt. Before adding it, checked whether the *existing* "Clear Local State" buttons (there were two — `#review` screen and Shadow Dungeon hub) actually worked. **They didn't.** Both called `state={};saveState();` — `saveState()` only writes `soloStudyingState_v1757`, a key a code comment already flags as dead-written-since-2026-06-15. Real progress lives in `cozy_arcade_progress_v1` via `phase3State.progress`, never touched by either button. This is exactly why "clear cookies/browser history" was the only thing that ever actually worked — the app's own button did nothing to real data, and no one caught it because the screen still visibly refreshed.

Fixed with one shared `window.clearAllLocalProgress()` (clears `phase3State.progress`, calls the canonical `savePhase3State()` — the same path every real rating writes through, so no second parallel write mechanism to drift out of sync). Both old buttons now call it, and both gained a confirmation step for the first time (plain `confirm()` — previously zero confirmation existed on either). Added a new "Advanced Settings" collapsible to the Settings & Gameplay drawer (matching the existing "How to Play" pattern) with a "Clear Local State" button and a custom inline Accept/Cancel confirm panel (not a native dialog, to match the drawer's own visual style), wired to the same shared function.

Live-validated: played and rated 3 cards, confirmed real progress present in both `phase3State.progress` and raw `cozy_arcade_progress_v1` localStorage; clicking Clear + Cancel left all 3 untouched; clicking Clear + Accept dropped both to 0; legacy key confirmed removed too; normal progress persistence across a hard page reload re-confirmed still intact (played a card, reloaded, progress still there — this was also the positive finding the user separately confirmed via their own testing on alternate devices); FSRS 17/17, smoke 6/6, zero errors.

## Shadow Dungeon modal text simplified (v109→v110, same commit)
Exact wording swap per user request: "Review pinned, missed, hard, or scheduled cards with the same timing as Solo Studying." → "Review scheduled, pinned, missed & random cards with auto-selection for time-crunch optimized review." One string, no logic touched.

## Two premortems completed, nothing coded (v109→v110, same commit)
Full detail in `GOAL.md`'s "UPDATE 2026-07-05 continued" section and `RECTIFIER_PLAN_2026_05_26.md`'s matching dated section. Summary:
1. **Shadow Dungeon "extend card review timing"**: traced `shadowDropSeconds351()` — it's a one-line passthrough to Solo's shared `questionSeconds351()`. The Shadow Dungeon setup modal's own "Game timing" dropdown does nothing; this is a third dead-dropdown finding this session (after `shadowSchedule351` and the duplicate modal). No evidence found that this was an intentional past workaround. Flagged, not fixed, pending a Shadow-Dungeon-specific regression pass given the user's own note about past timing-adjacent glitches in this area.
2. **"System Surge"/"Weak System Detected" integration**: traced the exact banner from the user's screenshot to a bare `showPulseToast(...)` call in `checkKnowledgePulse()` — confirmed the same `.pulseToast351` bottom-toast system already documented this session (not `vEnergyPulse`). Found `vEnergyPulse` already has unused `'SYSTEM SURGE'`/`'WEAK SYSTEM DETECTED'` message tables — partial, apparently-unintentional groundwork for exactly the consolidation the user wants, sitting dormant in a second system. Any real integration touches three systems at once (`.pulseToast351`, `vEnergyPulse`, `neuralPulse371`) and needs a deliberate canonical-system decision, not a reactive patch. Nothing touched.

**Documents touched this round:** this file, `RECTIFIER_PLAN_2026_05_26.md`, `GOAL.md`, `sw.js` (v110), `index.html` (Clear Local State fix, drawer addition, Shadow Dungeon text).

## Neural pulse v5 — real root cause of the disappear/reappear finally found and fixed (v108→v109)
User: "still flashing," specifically "Leveling… Session:…" text visible 1-3s, gone, reappearing later during card review — on a device confirmed already running the latest deploy (ruled out stale cache directly). This matched v3's already-fixed loop symptom too closely to be coincidence, so re-instrumented the exact answer→reveal→continue transition rather than re-testing idle state again (idle-only testing is what let this slip through both v3 and v4's validation).

Found it: a **second, independent trigger** for the same category of bug v4 only partially fixed. `watchGame()`'s `MutationObserver` fires on *any* mutation to `#solo`/`#domain`'s `class` attribute, not specifically on `hidden` toggling. Unrelated to the v4 body-childlist loop, `run()` (`renderHudControls`) still runs on its own independent schedule (a plain `setInterval(run, 500)`, line ~10329, that has nothing to do with what v4 fixed) and re-adds the `game-shell` class — which still fires a class-attribute mutation record even though the class's actual token set never changes. `watchGame()`'s naive "any class mutation → eject then inject" logic reacted to this every time, producing a real, brief (~50-100ms) disappear/reappear of the whole element right around answer/reveal moments — confirmed live via a dedicated instrumented test of the exact answer→reveal→continue cycle (not just idle sampling): one `classAttr` mutation logged at `t=8433.8`, followed immediately by `exists:false` at `t=8468.3`.

Fix: `watchGame()` now tracks the `hidden` boolean itself and only calls `eject()`/`inject()` when it actually flips, ignoring mutation records that don't represent a real state change. Re-ran the identical instrumented test after the fix: the same `classAttr` mutation still fires (proving the mutation source wasn't touched, only the reaction to it), but `exists` stayed `true` for all 60 samples. Extended to 5 full answer→continue cycles: zero disappearances. Star rotation and 12s idle-refresh crossfade re-confirmed unaffected. FSRS 17/17, smoke 6/6, zero page errors.

### What the diagnostic screen recording actually showed (recording itself deleted after use, per user request — not committed, never staged, this is the durable record)
User provided a ~16-18s screen recording (desktop Chrome, live site, 6 tabs open) to break a guessing loop after 4 blind revisions. What it showed, frame by frame:
- **0-2s**: a trail of small cyan dots following the mouse cursor across the screen — a separate decorative cursor-trail effect, unrelated to neural pulse, not investigated further (not what the user was reporting).
- **~5-16s**: the reveal panel ("GATE COMPLETED" + diagnosis title + purple/gold info boxes) stayed open and visually static for the remainder of the clip. Initial read of the compressed frames looked like the original question text was "ghosting" through the reveal panel's background — **this was a false lead**, caused by the extraction pipeline itself (avconvert transcode to 540p → re-trim to 0.05s clips → `qlmanage` JPEG thumbnail → `sips` 8x upscale — at least 2-3 generations of lossy compression, exactly the conditions that produce ghosting-looking artifacts in dark scenes with sharp text). Directly confirmed false via `getComputedStyle(#soloReveal)` on the live site (`backgroundColor: rgba(0,0,0,0)` is normal/expected — the visible solid box comes from inner content divs, not the outer container) and a fresh uncompressed screenshot showing a clean panel with no bleed-through. A higher-quality re-extraction (`PresetHighestQuality`, no downscale) of the same window confirmed: the original question text is simply visible in its own normal position *above* the reveal panel (which doesn't cover that area) — not overlapping it. Correcting this in the record since I initially treated it as a real finding before verifying.
- **The top status line itself was too small and too degraded by compression to visually confirm or rule out anything specific about the star or text in the recording** — this dead-end is what prompted asking the user for a precise timestamp instead of continuing to extract frames.

**What actually resolved this session's remaining bug was not the recording — it was the user's verbal follow-up** ("text flashing in and out... visible top from 1-3 seconds then later on card review") after confirming the device was already on the latest deploy. That description matched v3's already-fixed loop symptom closely enough to justify re-instrumenting the real answer→reveal→continue transition directly, which is what actually found the v5 root cause above. Lesson for future diagnostic sessions: a precise verbal description of *timing and location* from the user was more actionable here than a heavily-compressed recording — ask for both, but don't over-invest in extracting frames from a lossy recording when a specific instrumented test of the described transition will resolve it faster.

**Standing lesson, now proven twice on this exact feature (v3 and this fix): idle-state testing is not sufficient for anything reacting to DOM mutations.** Both v3's loop and this one only ever manifested around real state transitions (answer submission, reveal, continue) — a clean 2-second idle sample passed both times while the actual bug was live. Any future validation of mutation-observer-driven code in this file must instrument the specific transition it's supposed to react to, not just confirm steady-state behavior.

## Neural pulse v4 — star switched to continuous CSS rotation, no glyph-swapping at all (v107→v108)
User: "still flashing... should be like your terminal coding: rotation of progress... simple no actual flashing, gradation of a star." Even v3's smooth opacity-crossfade between different Unicode star glyphs (`·✢✶✻✽✻✶✢·`) read as "flashing" to the user — the fix wasn't a smoother crossfade, it was removing glyph-swapping entirely. Replaced with the exact mechanism this file already ships and the user has already approved for `vEnergyPulse`'s `.energyChipStar_v`: one static character (`✳`), a pure CSS `@keyframes` `transform:rotate(0→360deg) scale(1→1.15→1)` animation, `1.4s linear infinite` — zero JS touching the star's content or opacity, ever. Text content (session/due/energy stats, rating feedback) still crossfades on genuine content changes, since that's necessary information updating, not a "flashing" complaint.

While validating this, found and fixed a second real bug in v3: appending `#neuralPulse371` to `document.body` (needed for true `position:fixed` viewport-top placement) re-triggered an **already-existing** `MutationObserver` on `document.body`'s direct children (childList, non-subtree; part of the `run()`/`renderHudControls` cycle, ~800ms debounce) — which re-adds the `game-shell` class to `#solo`/`#domain`, which this feature's own `watchGame()` observer also reacts to (filters on the whole `class` attribute, not just `hidden`), creating a self-sustaining eject/inject loop. Caught via a 20-sample/2-second existence-continuity check (not visible in a single screenshot). Fixed by appending inside `#solo`/`#domain` instead — `.game` has no `transform`, so `position:fixed` children still measure from the true viewport, same visual result, loop gone.

Live-validated: 20/20 continuity samples (no loop), star's `textContent` never changes across samples while its computed `transform` matrix changes continuously (genuine smooth rotation, confirmed via computed style, not assumed from CSS), position `fixed` at viewport `y:0`, FSRS 17/17, smoke 6/6, zero page errors.

## Shadow Dungeon premortem → 2 of 4 findings actioned (v108, same commit)
Requested as analysis first given user's explicit "SM2 algorithm and card order very very delicate" framing. Findings and disposition:
1. **`shadowSchedule351` dead dropdown — removed.** Set in the setup modal's HTML (both duplicate copies), read by zero lines of JS anywhere in the file. Removed from both locations in one edit (byte-identical strings).
2. **`openShadowSetupFallback371()` duplicate — removed.** Byte-identical modal markup, simpler/less-complete Start handler. Its only call site's `else` branch was unreachable (primary assigns `window.openShadowSetup351` unconditionally, synchronously, much earlier in the file — confirmed via document-execution-order reasoning before deleting). Modal re-tested after deletion: opens correctly, exactly 3 dropdowns now (was 4).
3. **`.pulseToast351` — premortem claim corrected, NOT touched.** Initially called this dead code superseded by `vEnergyPulse`. Tested empirically (real wrong-answer through the UI) before acting: the bottom toast fired with live content, the top-right chip did not. Bare `showPulseToast(...)` calls (wrong-answer/persona/milestone messages) resolve to a different closure-scoped function than `window.showPulseToast` (which `vEnergyPulse` only overwrites at the `window` property, not the local variable other code in the same script references). **Two real, live, parallel feedback systems, not one dead one** — flagging for a deliberate consolidation decision, not deleting.
4. **SM2/card-pool filter logic — flagged, not scoped.** Confirmed independently reimplemented 5-6 times (`scope==='due'`/`'review'` at ~lines 1395, 3261, 4337, 4481, 6163, 6166), each with slightly different exact conditions. Needs a dedicated trace of which implementation is live for main-page Solo vs. Shadow Dungeon vs. Domain before any consolidation — not attempted this session.

Net code diff this round: -27 lines (index.html). FSRS 17/17, smoke 6/6, zero page errors, Shadow Dungeon modal re-tested after both removals.

**Documents touched this round (2026-07-05):** this file, `RECTIFIER_PLAN_2026_05_26.md` (new "2026-07-05 continuation" section — coding error log), `GOAL.md` (new "UPDATE 2026-07-05" section at top), `CLAUDE_HANDOFF_2026-07-05.md` (new — PHASE1's first dedicated handoff doc), `sw.js` (v108), `index.html` (star mechanism + loop fix + Shadow Dungeon cleanup).

## Neural pulse v3 — fixed a self-inflicted feedback loop, true viewport-top position, breathing star (v106→v107)
User reported "still flashing present" on live v106 and asked for (a) genuine top-of-viewport position independent of screen size, and (b) a smooth breathing-star cycle (like a CLI spinner: `·✢✶✻✽✻✶✢·`) instead of any abrupt swap. Root-caused the "still flashing": v2 was fine on its own, but repositioning to `position:fixed` led me to append the element directly to `document.body` — which re-triggered an **already-existing** `MutationObserver` on `document.body`'s direct children (childList, non-subtree, line ~13141) that debounce-calls `run()` at ~800ms. `run()` re-adds the `game-shell` class to `#solo`/`#domain` (`renderHudControls`), which this feature's own `watchGame()` observer also reacts to (filters on the whole `class` attribute, not just `hidden`) — creating a self-sustaining eject→inject loop. Measured directly: the element's existence flickered true/false roughly every 800-900ms, confirmed via a 20-sample/2-second continuity check before and after the fix (all `false` cycles gone after the fix, 20/20 `true`).

Fix: append inside `#solo`/`#domain` instead of `document.body` — `.game` has no `transform`, so `position:fixed` children still measure from the true viewport (same visual result), and insertion no longer triggers the problematic body-childList observer. Also added: smooth opacity crossfade on both the star (breathing through `·✢✶✻✽✻✶✢·` every ~420ms) and the main text (fades on the 12s idle refresh and on rating feedback) — no instant content swap anywhere now, matching the "gradation, not flashing" request directly.

Live-validated: 20/20 continuity samples over 2s (no more disappear/reappear), star opacity sampled mid-transition (0.21–0.99 range, confirming genuine crossfade not instant swap), position `fixed` at viewport `y:0` on both mobile and desktop, screenshot-confirmed clean at idle and immediately after rating (fireworks still visible, kept per earlier decision), FSRS 17/17, smoke 6/6, zero page errors.

**Standing lesson for this file:** `document.body`'s direct children are watched by at least one pre-existing `MutationObserver` tied into the `run()`/`renderHudControls` cycle. Any future code appending directly to `document.body` (not a descendant container) should check for this specific interaction first — it's an easy, non-obvious way to create a self-sustaining re-render loop.

## Neural pulse reimplemented (v2) — flashing removed, informative content kept, position fixed (v105→v106)
User clarified: wanted the flashing mechanics gone, not the informational text — "the text was cool, just wrong place." Reimplemented from scratch, keeping the exact same content (session time, due count, energy, per-rating stability/next-interval feedback) but removing everything that caused the original flicker complaint: no `.flash` color-transition class, no 4-character rotating-star cycling (replaced with one static `✳`). Repositioning took 3 iterations, each caught by live measurement rather than assumed:
1. First attempt: `top:0` inside `#solo`/`#domain` directly — visually collided with the HUD icon row (mobile HUD is a dynamic, content-dependent 117px-tall block, taller than assumed, leaving no free gap before the prompt box).
2. Second attempt: docked inside `.promptBox` as a normal-flow first child with `margin-bottom` spacing — measured actual gap to the system badge was **-5px** (real overlap) despite the margin value, because `.systemBadge` is `position:absolute!important` and completely ignores document flow/sibling margins.
3. Final: made the pulse element `position:absolute` too (same positioning context as the badge), anchored above it at `top:3px`. Measured gap to badge: **+2px** (clean). Zoomed screenshot confirmed genuinely no overlap — an earlier full-page screenshot had made a real 2px gap look like zero due to image-scaling compression, not an actual bug.

Live-validated both mobile (390×844) and desktop (1280×900) viewports via screenshot: text renders clean, legible, above the system badge and question, no flash class ever applied (checked directly on the DOM node after a rating), FSRS 17/17, smoke 6/6, zero page errors. Domain mode uses an identical `.promptBox` structure so the same fix applies there without separate positioning logic.

**Lesson for any future HUD/prompt-box layout work in this file:** several key elements (`.systemBadge`, likely others) are absolutely positioned with `!important` and completely opt out of normal document flow — a sibling's margin/padding has zero effect on them. Always measure actual rendered gaps (`getBoundingClientRect()`), never assume CSS box-model reasoning alone predicts the result in this codebase.

## Fireworks particle effect — investigated, kept as-is
User asked about a center-screen flash on every answer. Traced to `fireworks(x,y,color)` (line 420): 42 glowing "spark" particles burst outward from screen center (`innerWidth*.5, innerHeight*.62`) and fade over 0.75s — cyan for correct, red for wrong. Confirmed intentional (CSS: smooth `@keyframes spark` fade, not a glitch) via live DOM-mutation capture (all 37+ sparks spawned at the identical center coordinate, one clean batch). User's call: leave as-is, no code change.

## Gate-completion banner removed — real bug, not the fireworks (v104→v105)
Separately, user reported response text visible "behind the card" after answering. Traced via live screenshot + DOM capture to `#gateComplete`/`.gateBanner` (`runGateBanner()`, line 408): a "🏃 GATE COMPLETED" banner that runs left-to-right across the screen over 1.45s and stays visible 1.5s total — but fires on every correct answer alongside `reveal()`, which opens at a fixed 650ms delay (z-index 90) and covers the banner (z-index 20) mid-animation, every single time, no exceptions. Confirmed via screenshot: partial "...LETED" text visible bottom-left at 500ms, fully covered by 1300ms. Also fully redundant — the reveal panel's own title already displays "GATE COMPLETED". Presented as a genuine timing bug (not an occasional glitch) with three options (remove / fix z-index+timing / leave as-is); user chose removal, same pattern as the neural pulse fix. Removed all 3 `runGateBanner()` call sites (part of the `selectSolo` wrapper chain, same pattern as `fireworks()`'s calls), the function definition, the static `#gateComplete` HTML element, and the `.gateBanner`/`.gateBanner:before`/`@keyframes gateRun` CSS plus one mobile override rule. Live-validated: `#gateComplete` no longer exists in the DOM, reveal panel opens cleanly with no orphaned banner text, FSRS 17/17, smoke 6/6, zero page errors, screenshot-confirmed clean.

## Neural pulse flicker removed (v103→v104)
User reported distracting flashing during gameplay, provided exact source locations. Confirmed: `#neuralPulse371` (`vNeuralPulseCSS`/`vNeuralPulseJS`, ~line 14703) was a persistent, absolute-positioned text strip appended directly into `#solo`/`#domain` (not the HUD), positioned oddly (`top:52px`, under/near the hub), that mutated its text and a 4-character rotating star (`✳✻✽✶`) every 12 seconds, plus flashed a color change on every card rating via a 3rd wrapper around `window.rate` (explicitly commented "reads only, no state mutations" — purely cosmetic). Compared against `vEnergyPulse` (`window.showPulseToast`, ~line 14468) — the actual intended replacement pattern: a transient, fixed top-right chip with one smooth CSS-animated rotating star, auto-dismissing after ~4s. Confirmed via grep that neural pulse's DOM id/functions were referenced nowhere else in the file — fully isolated, safe to delete outright rather than patch or gate. Removed both blocks entirely (net negative ~120 lines).

**Full browser diagnostic run, self-executed (Codex out of credits this session) against the 10-item checklist, real Upload flow, mobile viewport (390×844):**
| # | Check | Result |
|---|---|---|
| 1 | `#neuralPulse371` null at idle / mid-question / reveal-open / post-advance | ✅ PASS, all 4 phases |
| 2 | No stray absolute-positioned pulse element under HUD at any point | ✅ PASS |
| 3 | No color-flash/star-mutation observable (implied by #1 — element never exists) | ✅ PASS |
| 4 | `vNeuralPulseCSS`/`vNeuralPulseJS` tags fully removed from DOM | ✅ PASS (count=0) |
| 5 | `.energyChip_v` appears on real wrong-answer flow through the UI | ⚠️ Did not fire in this scripted run — see note below |
| 6 | Milestone (5-correct) chip trigger, via real flow | ⚠️ Inconclusive — session-total undercounted due to test-loop timing, not the fix |
| 7 | `window.showPulseToast` called directly → `.energyChip_v` + rotating star appear | ✅ PASS (isolated, load-bearing proof the mechanism itself is intact) |
| 8 | `runFSRSValidation()` | ✅ PASS 17/17 |
| 9 | `runCozySmokeTests()` | ✅ PASS 6/6 |
| 10 | Zero console/page errors across full cycle | ✅ PASS |

**On items 5/6:** traced to a test-script artifact, not a regression — the app shuffles `choices` order per card (confirmed by inspection: `choices[0]` was not always the correct answer), so an early pass of the test script mis-selected answers by assuming a fixed index. After correcting the script to look up the actual correct/incorrect index each time, item 6's correct-count accuracy improved (0/3 → 3/3 correct detected) but the loop still only completed 3 of 5 intended cycles before the milestone threshold, consistent with pre-existing `selectSolo`-wrapper-chain/scripted-timing complexity already documented in this project (`ANSWER-SELECT-HEADLESS`) — not something introduced by this fix. Item 7's isolated direct call is the load-bearing evidence that `vEnergyPulse` itself is fully intact; items 5/6 test the *surrounding* trigger plumbing, which this fix never touched.

**Scope note:** user explicitly said no more changes to PHASE2 from this point — this doc and all fixes below are PHASE1-only. PHASE2's OPEN_DIFFERENTIALS.md/AGENTS.md are NOT updated with today's PHASE1-specific HUD work; if PHASE2 restrictions lift later, check whether PHASE2 needs the same fixes (it currently carries the same underlying bugs — see below).

## HUD fixes shipped today (sw v101 → v103)

**Duplicate/offscreen HUD home buttons (v101→v102, commit `fd85901`):** Two legacy functions, `addHudHome()` and `standardHud()`, each independently created their own untagged home button in the Solo/Domain HUD, uncoordinated with the canonical `ensureHudButton`/`renderHudControls` system. A slow (4s-interval) dedupe sweep cleaned them up eventually, but a real window existed right after starting a game where a duplicate button sat offscreen (`x:-5`). PHASE2 (reference copy at the time) had already deleted both button-creation blocks; ported that exact deletion — net negative lines, no new wrapper. Also found and fixed a second bug this exposed: with the legacy creators gone, the canonical home button was left invisible (a CSS rule hiding `[data-home]` buttons, written back when the legacy button provided the visible affordance, was never removed). Live-validated at 390×844: exactly 4 buttons, all on-screen, home button clickable, FSRS 17/17, smoke 6/6.

**Double-rendered Pause/Exit icons (v102→v103):** User supplied screenshots (`/Users/rebekahbetar/Documents/glitch source/HUD GLITCH/`) taken live, after the above fix was already deployed, showing the Home icon still appearing 0/1/2 times across rapid screenshots, plus older (2026-06-25) DevTools screenshots showing doubled `::before`/`::after` pseudo-element content on the Pause button. Live-instrumented computed styles (Playwright, real Upload flow, mobile + desktop viewports) found: `#soloPause` and the Exit/`[data-home]` button each had TWO separate icon-painting mechanisms active simultaneously — an old, hardcoded `::after{content:'Ⅱ'}` / `::after{content:'×'}` rule (from before the current icon system existed) stacking on top of the newer, unconditional `.hudIconButton371::before{content:attr(data-hud-icon)}` rule. Undo and Settings buttons (which never carried the old legacy classes) rendered clean with only one pseudo-element — direct proof the newer system alone is sufficient. Removed only the two old `::after{content:...}` declarations, left the accompanying `font-size:0` text-hiding rules untouched (they're not the cause, and are unconditional — verified they still correctly hide real text at both mobile and desktop viewports before and after the change). Live-validated: `::after` computed content is `none` on every HUD button now, single clean icon render, screenshot-confirmed, home-button click still works, FSRS 17/17, smoke 6/6, zero page errors.

## PHASE2 comparison, for the record
**Update 2026-07-05: restriction lifted, double-icon fix ported.** Codex independently audited PHASE2 live and found the identical `::after` double-paint bug. User explicitly approved lifting the "no more PHASE2 changes" restriction for this one fix. Applied the same 2-line deletion to PHASE2 (sw v67→v68), live-validated the same way (computed styles, home-button click, FSRS 17/17, smoke 6/6). Full detail in PHASE2's `OPEN_DIFFERENTIALS.md` → `HUD-DOUBLE-ICON-PAINT`.

- The duplicate-creator bug: PHASE2 already fixed, months ago — this is where the PHASE1 fix's deletion pattern came from.
- The double-icon `::after` bug: **PHASE2 has the exact same unfixed rules** (`#soloPause::after,#domainPause::after{content:'Ⅱ'...}` and `#solo .hud [data-home]::after...{content:'×'...}`, confirmed via read-only grep, both still present). If/when PHASE2 work resumes, the identical two-line deletion applies there too.

## Aesthetic/UX goal, for context on all HUD work
Per prior sessions' `GOAL CLARIFIED` note: this app is meant to work smoothly for OTHER users, not just as a personal study tool — glitches like flickering/doubled icons are exactly the kind of thing that erodes trust in an unfamiliar app faster than a missing feature would. HUD work should aim for: one visible control per role, no visual doubling/ghosting, consistent icon rendering across breakpoints. Full reveal-panel consolidation (the bigger, still-open architectural item) is a separate, larger effort — see PHASE2's RECTIFIER_PLAN 2026-07-04 addendum for that plan; not part of today's PHASE1-only HUD work.
